#include <stdio.h>
int main()
{
    int h,n,ele,i;
    scanf("%d",&n);
    i=1;
    while(i<=n)
    {
        scanf("%d",&ele);
        if(i==h)
        h=ele;
        if(ele>h)
        h=ele;
        i++;
    }
    printf("Highest number %d",ele);
    return 0;
}
